using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

namespace Assets.Scripts
{

    public class DataLayer : MonoBehaviour
    {

      
        private const string filepath = @"..\JSON\";














    //    private List<object> JSONreader(string[] objectsToGet, container myContainer)
    //    {

    //        string filename = filepath + myContainer;
    //        string text = File.ReadAllText(filename);

    //        if (myContainer.Equals(container.enemies) {

    //            var objectList = JsonUtility.FromJson<EnemyWrapper>(text);

    //        }
    //        else
    //        {
    //            objectList = JsonUtility.FromJson<CardWrapper>(text);
    //        }

          
    //        var objectReturn = new List<object>();


    //        foreach (string objectToGet in objectsToGet)
    //        {

    //            var obj = objectList.EnemyItems.Find(item => item.EnemyName.Equals(enemytoget));
    //            EnemyReturn.Add(enemy);

    //        }

    //        return EnemyReturn;

    //    }
    }
}
